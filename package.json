{
  "name": "sass-project",
  "version": "1.0.0",
  "description": "heh, sass, heh, authentication and authorization",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "compile-sass": "sass sass-files:src/css --no-source-map",
    "compile-watch-sass": "sass sass-files:src/css --no-source-map --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.54.3"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1"
  }
}
